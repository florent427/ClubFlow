# URL HTTP du endpoint GraphQL NestJS
VITE_GRAPHQL_HTTP=http://localhost:3000/graphql

# Optionnel : pré-remplit le champ « Identifiant club » sur la page de connexion (UUID affiché par npm run db:seed)
# VITE_DEV_CLUB_ID=
